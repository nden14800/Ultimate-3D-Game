# Phase 4 — NPC行動AI・ローカル会話 検証記録

ローカル環境でプレイ開始後、Conversation Dockに**「ローカルAI: 未起動」**と明示的な「LOCAL AIを開始」ボタンが表示されることを確認した。起動直後にWebLLMモデルの取得は行われず、ユーザーによる明示的操作まで外部モデルのロードを保留する設計になっている。

NPC活動表示は、単純な人数表示ではなく、`10人が移動・2人が会話中`のように巡回・散策・回避と会話状態を集約した内容へ更新されている。ワールド内のNPCも道路・歩道を目的地として移動し、会話中のNPCには吹き出しが表示される。

会話欄から「高い建物を作るコツは？」を送信したところ、LOCAL AI未起動状態でもプレイヤーの発言と近隣NPCの返答がConversation Dockとワールドの吹き出しに反映された。WebGPU非対応・モデル未起動・ロード失敗時に、ゲーム全体を止めずテンプレート会話を継続するフォールバックは動作している。
