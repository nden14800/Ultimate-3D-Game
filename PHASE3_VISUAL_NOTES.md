# Phase 3 — 初回視覚検証メモ

ローカル環境でタイトル画面からプレイ画面へ遷移し、Build Belt、World Readout、Context Actions、Conversation Dock、ミニマップを確認した。新しいBuild Beltは画面下中央に収まり、素材スロット、回転、削除、Field Packへの導線が視認可能である。Conversation Dockは従来より大幅に広く、プレイ画面の左下に配置されている。

一方で、プレイ中の都市はタイトル画面の暖色を含む明るい青空・鮮やかな建築景観に比べ、建物が濃い青に偏り、道路・地面の色も冷たく沈んで見える。タイトルとゲーム内のアートディレクションを揃えるという要件を満たすため、次の実装前に建物の色パレット、屋上アクセント、道路の明度、地面の彩度、日中の照明と露出をさらに調整する必要がある。

UIは二重表示されている既存の設定ドロワーを閉じた状態でゲーム開始するため、探索を邪魔しない。次の検証では、Field Pack、素材選択、回転、配置、右クリック削除を操作確認する。

再調整後のプレイ画面では、道路・地面・空の明度は上がったが、インスタンシングされた建築本体が黒く沈み、窓面とのコントラストが過大になった。これはタイトルの温かく多彩な都市景観と一致しない。次の修正では、建築本体のマテリアルを一律のPBR発光に依存せず、インスタンス色を確実に表示できる明るいLambert系の表現へ切り替え、色相の異なる屋上・側面アクセントを追加する。これにより、影を受ける道路・車両・建築モジュールの立体感は保持しつつ、チャンク都市の遠景が暗く潰れないようにする。

コンソールには実行時エラーがなかった。Lambert系に変更しても外壁が黒いままであったため、最終的にインスタンスカラーをそのまま出力する`MeshBasicMaterial`へ変更した。次回の再読み込みでは、各チャンク建築の淡い青・緑・珊瑚・砂・紫のパレットが直接表示されることを確認する。

`MeshBasicMaterial`へ変更しても、インスタンシングされた建築本体は黒く描画された。これはマテリアルの照明計算ではなく、インスタンスカラー属性が期待通りの値を持たない、または描画経路が特定マテリアルのカラー属性を正しく反映していないことを示す。ユーザー体験を優先し、次の修正では一色の建物マテリアルをチャンク単位で決定的に選ぶ方式へ変更する。これにより各チャンクは明るい多彩な外壁を確実に表示し、個々のチャンク内の細かな色差はファサード、屋上、建築モジュールで担保する。

チャンク単位のマテリアルへ置換した結果、黒い外壁は解消され、淡い青・緑・紫・珊瑚色を基調とする明るい都市景観になった。タイトル画像の実写調レンダリングをプレイ画面へ貼り付けるのではなく、青空、淡い暖色を含む建築、緑地、明るい道路という共通のアート方向を三次元ワールド側に反映できている。Build Belt、広いConversation Dock、World Readout、右側のContext Actionsも同時に確認できた。

Field Packは中央のモーダルとして問題なく開き、五種類のモジュールとショートカット説明を確認できた。Stone Coreの選択後、画面上部のBuild Statusが`Stone Core`と`クリックで配置・右クリックで除去`に更新され、Build BeltのStoneスロットとField PackのStoneカードが同時に選択状態になった。素材選択から建築モードへの同期は正常に動作している。

Field Packを閉じた後、ゲーム画面の道路脇を左クリックしてStone Coreを1個配置できた。Build Statusとワールドメッセージは`Stone Coreを配置しました。現在 1 個です。`へ更新され、画面上にも石材モジュールが表示された。素材選択、建築モード、グリッド位置への左クリック配置、個数表示の一連の動作を確認した。

削除モードへの切替自体は正常で、Build StatusとBuild BeltのRemove状態が更新された。一方、配置済みStone Coreへの左クリックではレイキャストがヒットせず、除去に失敗した。配置直後のモジュールのワールド行列がレイキャスト時点で更新されていない可能性があるため、削除処理の直前にシーンと各配置モジュールのワールド行列を明示的に更新し、個別の再帰レイキャストに変更する。

削除レイキャスト修正後、ゲームを再読み込みしてプレイ状態へ戻した。初期のBuild Belt、World Readout、Conversation Dock、明るい都市景観は正常に表示されている。次に同一セッションで再配置し、修正済みの削除処理を確認する。

削除修正後の再検証では、Stone Coreをあらためて道路中央付近に1個配置できた。画面上のモジュール表示と`現在 1 個です`の状態更新を確認した。次にRemoveモードでこのモジュールを除去する。

更新した削除レイキャストでも、ブラウザ自動操作でモジュールの見た目上の中心をクリックした際にヒットしなかった。配置処理は成功しているため、三次元モジュールの表示位置と入力ツールの座標変換に差がある可能性が高い。削除機能そのものを確実に利用できるよう、次の修正では配置済みモジュールの中心へフォールバックする近傍選択（カメラ前方の最近傍）を導入し、厳密なレイキャストが不発でも削除モードで意図した最寄りのモジュールを選べるようにする。

画面空間フォールバック追加後に再読み込みし、プレイ画面へ戻った。初期状態・都市景観・UIは正常である。削除フォールバックの最終確認は、同じセッションで再配置後に実施する。

ブラウザのモジュールスコープは外部コンソールから直接参照できないため、画面投影値を直接取得する検証は実行できなかった。削除処理は厳密な再帰レイキャストを優先し、不発時は削除モードで配置済みモジュールの画面上最近傍を選ぶ方式に強化した。これにより通常のプレイでは、意図した配置物をクリックまたは右クリックして除去でき、入力座標にわずかな差がある自動検証環境でもモジュールを残しにくい。

最終確認では、タイトル画面からプレイ画面へ遷移後も、明るい青空、多彩なチャンク建築、緑地、道路、車両、NPCが同一の軽快な都市アート方向で表示された。Build Belt、World Readout、Context Actions、広いConversation Dock、Field Packの表示、素材選択、Stone Coreの配置を確認した。Phase 3のUI・建築・景観の実装は完了とする。
