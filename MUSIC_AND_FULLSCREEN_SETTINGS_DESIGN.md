# オリジナルBGMと全画面World Settings設計

## 方針

BGMには、第三者配布曲ではなく、本更新専用に生成するインストゥルメンタル曲`Neon City Drift`を使う。歌詞、ボーカル、既存のアーティスト名、既存曲を示す参照はプロンプトへ含めない。ゲーム側はユーザーの最初の「街へ入る」操作を再生許可として扱い、`Audio`要素を遅延作成する。自動再生禁止を回避しつつ、設定でオン／オフと音量を即時に反映する。

| 状態 | BGM動作 |
|---|---|
| タイトル表示 | 再生しない。 |
| ゲーム開始後、BGMオン | ループ再生を開始する。 |
| ポーズ／全画面設定 | BGMは低い音量へダッキングし、ゲームへ戻ると設定音量へ復帰する。 |
| タイトル復帰・BGMオフ | 一時停止する。 |
| 音源読込失敗 | ゲーム本体を止めず、設定に再試行可能な状態を表示する。 |

## 全画面設定

`World Settings`は従来の右側カード型`WORLD`タブではなく、ゲーム上に重ねる全画面オーバーレイとして実装する。左側に目的と状態、右側に操作領域を置き、モバイルでは縦一列へ切り替える。既存のWORLDカードは非表示にし、設定機能はオーバーレイへ移す。

| セクション | 操作 | 既存システムへの接続 |
|---|---|---|
| World Atmosphere | 天候、時間 | `changeWeather`、`updateTime` |
| Render Profile | 品質、描画チャンク | `setQuality`、描画距離更新 |
| City Activity | 交通車両 | `trafficEnabled` |
| Soundtrack | BGMオン／オフ、音量、曲名、クレジット状態 | `Audio`要素、音量スライダー |

## 到達経路

ポーズハブの「ワールド設定」は全画面設定を開き、ポーズハブを閉じる。`WORLD`タブとContext Actionsのマップは、従来の小型カードを開かず、必要に応じてWorld Settingsまたはレーダーを扱う。設定画面の「ゲームへ戻る」でオーバーレイを閉じる。モバイルではポーズ、World Settings、すべてのスライダーとトグルを操作可能にする。

## 音源ファイルと権利表示

生成後の音源は`assets/audio/neon-city-drift.mp3`として保存する。`LICENSE-ORIGINAL-BGM.md`には、ゲーム用に生成したオリジナルのインストゥルメンタル音源であること、NCSを含む第三者配布曲を使用していないこと、音楽生成サービスの利用条件を確認することを記載する。
