# Interaction, Interior, Collision, and Multi-Agent Design

## 実装上の原則

本更新では、描画距離と既存の自由探索を維持するため、遠景はインスタンシング、近傍で相互作用する対象は通常メッシュとデータ登録を使い分ける。衝突は物理エンジンを導入せず、近傍チャンクに限定した二次元AABBで判定する。これは無限都市での毎フレーム負荷を抑え、プレイヤー・車両・配置物が建物をすり抜けないというゲーム上の期待を満たす。

| 領域 | 設計 |
|---|---|
| 車両方向 | 車両も徒歩と同じカメラ相対の前後・左右入力を使う。画面の上方向が常に前進で、前進ベクトル・向き・移動量を同じ値から計算する。 |
| 衝突 | チャンク建物、City Hubの外壁、配置済みモジュール、乗車していない車両、屋内壁を`collisionBoxes`へ登録する。候補位置が衝突した場合はX軸・Z軸を個別に解決し、壁沿いの移動を可能にする。 |
| 建物内探索 | 決定論的に近傍のCity Hubを生成する。入口の近くで`E`または`Interact`を使うと、屋内ロビーへ移動する。屋内は床、壁、柱、窓、照明、座席、観葉植物、退出ゲートを持つ。 |
| 操作の同等性 | キーボードの`E/B/R/7/Tab/Esc`に対応する乗降・入室、建築、回転、削除、Field Pack、ポーズの画面ボタンを追加する。キーボード入力時はモバイル移動UIを隠し、タッチ・ポインタ入力時は再表示する。 |
| 全画面メニュー | ポーズボタンまたは`Esc/P`で、設定・World Tools・Vehicle Garage・Social・Field Pack・操作ガイドを全画面City Pause Hubにまとめて表示する。 |
| 会話欄 | デスクトップは横幅を広げ、モバイルは展開時に上部から十分な高さを取りつつ、Build Belt・移動UIと干渉しない。会話ログは長文を折り返し、ヘッダーを固定する。 |
| 複数ローカルAI | 1つのWebLLMエンジンに`localAiQueue`を設け、各NPCの個別履歴・役割・会話相手を保持する。AI起動後は近接NPCペアの自律会話をキューへ追加し、プレイヤー入力は最優先キューとして扱う。 |

## 入力制御

最後にキーボードを使った時刻を保持し、粗いポインタを持つ端末では3秒間モバイルのジョイスティックを非表示にする。タッチ開始またはポインタのタッチ入力で直ちに再表示する。Build BeltやContext Actionsは、入力方式に関わらず維持する。モバイルでは`Interact`、`Build`、`Rotate`、`Remove`、`Pack`、`Pause`、`Camera`、`Vehicle`、`Chat`を直接利用可能にする。

## City Hubとリアリズムの対象範囲

City Hubは各プレイヤー近傍で一つだけ有効化する、アクセス可能な高密度建築である。外観は階層化されたポディウム、窓スパンドレル、ガラスのカーテンウォール、屋上機械設備、庇、入口、照明、植栽で構成する。車両はタイヤ、ホイール、ライト、バンパー、独立したキャビン、スポイラーまたはホバーユニットなどを追加する。全ての細部を全チャンクに置かず、City Hubとプレイヤー車両にだけ配置して性能を保つ。

屋内ロビーは屋外の座標から切り離した専用区画に置き、入口・出口でフェードなしの座標遷移を行う。外観を視認したまま「建物の中」に入れる操作として扱い、屋内と屋外の双方に衝突壁を登録する。車両のままの入室は避け、入室時には降車を案内する。

## 検証条件

1. 徒歩と車両の両方で、画面上方向の入力が前進になる。
2. 建物・配置物・車両・屋内壁のいずれもすり抜けない。
3. `E`とモバイルInteractの両方で乗降または入退室できる。
4. `B/R/7/Tab/Esc`の全機能に同等の画面ボタンがある。
5. キーボード利用時にモバイル移動UIが隠れ、タッチで再表示される。
6. ポーズから全画面メニューが開き、ゲームを止めたまま各メニューへ移動できる。
7. LOCAL AIを起動したとき、複数NPCが自律会話し、プレイヤーの発話には近傍NPCが優先応答する。
8. WebGPU未対応時は、複数NPCのテンプレート自律会話へ安全にフォールバックする。
