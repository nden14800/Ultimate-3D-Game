# Ultimate 3D City — Creative Sandbox Refresh 設計

## 設計目的

本刷新は、既存の自由探索、無限チャンク都市、車両、天候、テレポート、自動運転および品質プリセットを保持したまま、建築・NPC・会話を主役にした創作都市サンドボックスへ発展させるものである。既存ゲーム固有のUIやアセットを模倣せず、ゲームUIにおける情報優先度、文脈に応じた操作提示、視覚言語の一貫性という一般原則を採用する。[1]

> プレイヤーが常に必要とするのは「現在の場所」「選択中のツール」「次に実行できる操作」である。詳細設定は常設しない。

## 画面構成

| 領域 | コンポーネント | 表示内容と操作 |
|---|---|---|
| 左上 | `World Readout` | 位置、移動速度、描画チャンク、昼夜を小さく提示する。 |
| 上中央 | `Build Status` | 現在の素材、回転角、配置モード、削除モードを短く示す。 |
| 右上 | `City Radar` | 既存ミニマップを維持し、目的地・車両・NPCの位置を表示する。 |
| 画面下中央 | `Build Belt` | 数字キー1–8とクリックで即時選択できる8枠の独自クイックバー。素材、回転、削除、乗り物、会話、Field Packの入り口を配置する。 |
| 右下 | `Context Cards` | 近くの車両、NPC、配置モードの補助操作を示す。状況に応じて内容を切り替える。 |
| 左下 | `Conversation Dock` | 従来より広い会話ドロワー。ローカルAIの状態、会話履歴、入力欄を常設または折り畳みで表示する。 |
| 中央 | `Field Pack` | `Tab`またはBuild Beltから開く半透明のインベントリ。素材、設計、ワールドの三面構成とする。 |

## 建築システム

建築はランダムサイズ・ランダム色のプロップ配置から、グリッドスナップされた独自ブロックモジュールへ移行する。素材は屋外都市に馴染む五種を用意する。各モジュールは`THREE.Group`として管理し、配置済みオブジェクトには建築ID、素材ID、回転、衝突半径を保持する。

| 素材ID | 表示名 | 役割 | 表現 |
|---|---|---|---|
| `turf` | Turf Deck | 緑化された基礎・公園の床 | 芝色、粗いマット質感、低い縁取り。 |
| `stone` | Stone Core | 石材の壁・段差 | スレート色、やや荒い反射。 |
| `timber` | Timber Stack | 木製の梁・小屋 | 暖色の木調、水平帯のディテール。 |
| `glass` | Clear Prism | 窓・温室・展望塔 | 半透明、青緑の縁、弱い発光。 |
| `alloy` | Alloy Module | 近未来的な構造物 | 青銀色、金属反射、シアンのアクセント。 |

標準操作は左クリックで配置、右クリックで削除、`R`で90度回転、`B`で建築モード切替、数字キーでBuild Belt選択、`Tab`でField Pack表示とする。建築モード中は、レイキャスト結果に合わせて半透明のゴーストを表示する。ゴーストの色は有効な地面ならシアン、既存ブロック・車両・NPCに近すぎる場合は赤とする。これは視覚的フィードバックによって誤配置を予防するためである。

## 都市表現

タイトル画面の画像をプレイ中に重ねるのではなく、プレイ可能な三次元都市のライティング、空、地面、建築色を明るい低ポリ都市景観へ調整する。青い空、暖色の太陽、青緑のガラス、白・砂・珊瑚・青を基調にした建物パレット、緑化された地面を共有し、タイトルとゲーム内で同一のアートディレクションを感じさせる。

描画の軽さはインスタンシング、既存のチャンク距離、影キャスターの近傍制限、品質プリセットを維持して確保する。新規建築物は少数であるため個別メッシュとして扱い、上限を設けてメモリ使用量の無制限な増加を防ぐ。

## NPC行動設計

NPCは`wander`、`patrol`、`wait`、`avoid`、`talk`の状態を持つ。各NPCは道路交差点、歩道、または近隣の関心地点を目的地として選び、到着後に一定時間待機する。周囲のNPC、プレイヤー、車両、配置物との近接距離を確認し、混雑時には小さな回避ベクトルを加えて進行方向を調整する。遠方に離れたNPCは、既存の描画距離に合わせてプレイヤー周辺へ安全に再配置する。

## ローカルAI会話設計

NPCのローカルAIはWebLLMを用いた**任意起動機能**とする。会話を初めて開始する時だけ、WebGPU検査、モジュールの動的読込、モデルロードを行い、進捗をConversation Dockへ表示する。モデルはブラウザ内で実行され、アプリケーションのサーバーやAPIキーを使用しない。一方、初回ロードには時間と端末容量を要するため、ロード中・非対応・失敗時は軽量な端末内テンプレート会話へ確実にフォールバックする。[2]

会話に使用するシステム指示は、NPCが創作都市の居住者として日本語で短く応答し、現実の人物・権限・外部情報を偽装しないよう制限する。応答はストリーミングで段階表示し、送信中は入力欄の送信ボタンを無効化する。

## 互換性と検証条件

| 領域 | 必須条件 |
|---|---|
| 自由探索 | WASD・矢印・モバイルジョイスティックによるカメラ相対移動を維持する。 |
| 車両 | クルーザー、スポーツカー、ホバーバイクの出現・乗降・操縦を維持する。 |
| 無限世界 | 64×64チャンクのストリーミング、1R/2R/3Rの描画距離を維持する。 |
| 画質 | PCFソフト影、色空間、トーンマッピング、軽量・プレミアム・ウルトラ品質を維持する。 |
| UI | Bootstrap Icons v1.13.1のローカルSVGスプライトを継続利用する。 |
| AI | 起動前にモデルを取得しない。WebGPU未対応時もゲーム全体を正常に動作させる。 |
| 配布 | 単一HTMLと静的アセットの構成を維持し、Cloudflare Pagesでビルドなしに配信する。 |

## 参照

[1] [Roblox Creator Hub — UI and UX design](https://create.roblox.com/docs/production/game-design/ui-ux-design)

[2] [WebLLM — Basic Usage](https://webllm.mlc.ai/docs/user/basic_usage.html)
