# Phase 16 — Production Bug Reproduction

本番URL `https://ultimate-3d-game.pages.dev/` で標準のHARBOR HEARTへ参加して不具合を再現した。

| 項目 | 観測結果 |
|---|---|
| 地面 | 広範囲が不自然な白一色になり、道路と地面の質感・境界が実質的に失われている。 |
| 建物 | 大半が非常に白く単調で、都市のPBRファサードや以前の街並みの品質が失われている。 |
| キャラクター | CC0キャラクターが青いウサギ状の見た目で強く目立ち、元の街の美術方向と整合していない。 |
| HUD | HUD・地図・Build Belt・会話ドックが重なり、プレイ領域を圧迫している。 |
| 再現条件 | 標準設定でタイトル画面からHARBOR HEARTへ参加するだけで発生。 |

次にコンソールと材質・外部モデル統合の初期化順序を確認し、地面・建物・キャラクターの適用方法を安全なフォールバック方式へ戻す。
| コンソール | 実行時例外・警告は観測されなかった。 |
| 診断アクセス | ES Module内の`scene`・`renderer`はグローバルに露出しておらず、コンソールから直接のシーングラフ解析はできない。 |
| 天候 | 本番での選択値は`sunny`であり、白地面は雨・雪プロファイルではなく、モデル・材質・HUDの統合に由来する可能性が高い。 |
| 安定待機 | 参加後に十分待機しても`STREAM`が「読み込み中」のままで、白地面・単調な街並みは解消しなかった。 |
| 短時間移動 | 本番ページで800msの`W`入力を与えても座標HUDが`X: 0.0, Y: 0.0, Z: 0.0`のまま。毎フレーム更新処理が動いていない、または最初のフレームで停止している可能性が高い。 |
| WebGLキャンバス | `game-canvas`は固定配置でビューポート全体（1280×1100）を占め、サイズ・位置は正常。 |
| City Atlasレイヤー | `#city-atlas-screen`は`hidden`クラスにより`visibility:hidden`、`opacity:0`、`pointer-events:none`。Atlasは白い領域の原因ではない。 |
| 再現監視 | 本番ページを再読み込みし、参加前に`error`と`unhandledrejection`の監視を設定した。次の参加操作で初期フレーム例外を捕捉する。 |
| 監視下の再現 | エラー監視を設定した後も、標準参加直後に白い地面、都市材質の喪失、`STREAM: 読み込み中`、静止したHUDを再現した。 |
| 修正版ローカル検証 | NPCの`targetPosition`とAI状態を外部モデル差し替え時に保全する修正を適用し、構文・Phase 10・11・14の全静的検証を通過した。 |
| 修正版結果 | 合格。`STREAM`は`25 chunks · 2R`へ更新され、道路・地面・街区が描画され、NPC活動表示と会話も更新した。白い空白地面と停止は解消した。原因は外部キャラクターへ差し替えたNPCから`targetPosition`が失われ、`moveNpcs()`が最初のフレームで例外停止したことだった。 |
| 継続フレーム確認 | 1.2秒待機後も`25 chunks · 2R`とNPC活動が更新され、例外は0件。 |
| 実移動確認 | 650msの`W`入力で座標は`Z: 0.0`から`Z: -0.4`へ変化し、例外は0件。 |
