# 無限都市ワールド設計

## 目的

固定された `MAP_WORLD_SIZE` を廃止し、プレイヤーまたは操作中の車両の周辺だけを生成する、決定論的なチャンクストリーミングへ移行する。ワールド座標に上限は設けず、移動、乗り物、NPC、配置、ミニマップ、天候、品質設定をチャンク境界をまたいで維持する。

## チャンクモデル

| 項目 | 設計 |
|---|---|
| チャンク寸法 | 64×64ワールド単位 |
| 識別子 | `cx,cz` の整数座標。`Math.floor(position / CHUNK_SIZE)` から決定する。 |
| 生成 | 座標由来の疑似乱数を使い、同じ座標なら毎回同じ道路・建物・植栽を生成する。 |
| 読み込み | 操作主体を中心に、描画距離で定めた正方形のチャンク群を生成する。 |
| 破棄 | 保持半径の外に出たチャンクのグループをシーンから外し、チャンク固有のジオメトリとマテリアルを `dispose()` する。 |
| 描画距離 | 軽量: 1、プレミアム: 2、ウルトラ: 3 チャンク半径。保持半径は描画半径に1を加える。 |

## 都市生成

道路はグローバル格子に沿わせるため、チャンクごとに別々の道路が途切れない。建物、窓、樹木、街灯は各チャンク内で `InstancedMesh` にまとめる。建物はチャンク単位のグループに入れ、不要になったグループだけを破棄する。これにより、無限性を実現しながら、常時保持するメッシュとGPUメモリを描画距離に比例させる。

## 既存機能との連携

| 機能 | 無限ワールドでの対応 |
|---|---|
| 徒歩・車両 | 境界クランプを撤去し、操作主体の座標に応じてストリーミングを更新する。 |
| NPC | プレイヤー近傍でのみ活動させ、距離が離れたNPCは現在地周辺へ再配置する。 |
| 交通 | グローバル道路格子上を移動させ、保持範囲の外では近傍道路へ再配置する。 |
| ミニマップ | プレイヤー中心のローカルレーダーに変え、近傍のチャンク・道路・車両・NPCを表示する。 |
| テレポート | レーダー上の相対位置をワールド座標へ変換して移動し、到着先チャンクを直ちに読み込む。 |
| 自動運転 | 建物衝突グリッド依存を外し、グローバル道路格子を経由する経路を使う。 |
| 配置 | 既存の手動配置物はワールド座標で保持し、チャンク境界をまたいでも消さない。 |
| 天候 | 操作主体へ追従し、粒子領域が無限移動でも常に周辺を覆うようにする。 |

## 検証指標

各品質プリセットで、読み込みチャンク数、保持チャンク数、レンダーコール、FPSをHUDに表示する。数百ワールド単位の徒歩・車両移動を行い、チャンクが生成・解放され、座標クランプやメモリ増加が発生しないことを確認する。

## 参考資料

[1] [Three.js — InstancedMesh](https://threejs.org/docs/pages/InstancedMesh.html)

[2] [Three.js — Cleanup](https://threejs.org/manual/en/cleanup.html)

[3] [Three.js — WebGLRenderer](https://threejs.org/docs/pages/WebGLRenderer.html)

[4] [Three.js — Object3D](https://threejs.org/docs/pages/Object3D.html)
