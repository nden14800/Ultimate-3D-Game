# Ultimate 3D Sandbox — Mega Refresh Design

## 設計目的

既存の無限チャンク、建築、車両、City Hub、ローカルAI、モバイル操作を維持しつつ、ゲーム中の情報密度を下げ、目的別の操作を全画面へ移す。UIは既存作品のコピーではなく、青系の都市地図、白い立体カード、シアンの主操作、近接プロンプトを共通する独自の視覚言語とする。設計原則は、文脈に応じた操作だけをHUDへ残し、詳細な操作を全画面へ移すことである。[1]

## 全画面画面の共通契約

| 画面 | 開き方 | 主な内容 | 閉じた後 |
|---|---|---|---|
| City Pause | ポーズ、`Esc`、`P` | 再開、詳細マップ、ワールド、ガイド、建築、車両、会話、タイトル | 探索再開 |
| Guide Atlas | ポーズ内のガイド、タイトルのガイド | 入力方式別の操作、近接行動、カメラ、建築、交通、AI | 元の画面へ戻る |
| World Settings | WORLDタブ、ポーズ内のワールド | 天候、時間、描画距離、品質、影、音量、AI品質 | 探索再開 |
| City Atlas | マップボタン、ポーズ内の詳細マップ | 高解像度地図、ズーム、パン、目的地、テレポート、自動運転、レイヤー、地点情報 | 探索再開 |
| NPC Conversation | NPC近接の使う操作、NPCマーカー | 人格、会話履歴、LOCAL AI状態、送信、モデル選択 | 元の画面へ戻る |

すべての全画面には固定の右上閉じるボタン、モバイル安全領域、背景HUDを完全に覆う最高優先度レイヤー、主操作を1つに絞った下部アクションを持たせる。

## HUDとモバイル

ゲーム中には、世界読み出し、Build Belt、文脈アクション、コンパクト化したマップ／サンドボックス展開ボタン、会話ドックのみを残す。`ULTIMATE 3D SANDBOX`の大きいカードは常時表示せず、右側の小型「CITY DESK」ボタンで表示・非表示を切り替える。ミニマップは右下ではなく、モバイルの走るボタンと重ならない右上寄りの安全領域に再配置し、ワンタップでCity Atlasを開く。WORLD READOUTは2列カードを縦に折り返し、座標を小数1桁と短縮表記へ変更して切れを防ぐ。

モバイルには、走る、使う、建築、マップ、視点、ズーム＋／－、ポーズを独立した操作として配置する。キーボード入力を検知したときだけ移動用ジョイスティックを隠し、全画面のタッチ操作は維持する。

## City Atlas

ミニマップと別に、1024px内部キャンバスで描く詳細地図を追加する。地図の中心はプレイヤー／操作中車両で、ズーム範囲は0.55–5.0、パン、北固定／進行方向固定、道路、建物輪郭、City Hub、目的地、NPC、車両、交通、公共交通停留所、配置物をレイヤーとして切り替えられる。クリックまたはタップで目的地を置き、テレポートと「徒歩で移動」「車で自動運転」のどちらかを明示的に選ぶ。

経路計算は道路グリッドを使用し、車両が制御中ならその車両の位置を開始点とする。既存の徒歩用経路を車両でも更新し、交差点手前の減速、目的地での停止、衝突時の再経路計算を追加する。

## カメラ

`Free Look`を廃止して`Free Cam`へ置換する。視点の循環は三人称、第一人称、Free Camとする。三人称・第一人称はホイール、ピンチ、HUDの拡大／縮小でFOVを調整し、三人称ではキャラクター中心のカメラ距離も調整する。Free Camへ切り替えると、ワールド原点へ戻らず、プレイヤーまたは車両の現在位置を中心とした位置と注視点で開始する。Free CamはWASD／ジョイスティックで浮遊移動、ドラッグで回転、上下移動ボタンで高度調整する。

## 車両・交通・速度

車種はクルーザー、スポーツカー、ホバーバイクに加え、シティバス、路面電車、配達バン、タクシーを追加する。各乗り物には最高速度、加速、減速、旋回、車幅、席数、交通用の通常速度を設定する。速度はゲーム座標をメートル相当としてHUDに実測値を表示する。車両は即座に指定方向を向くのではなく、操舵角と速度依存の旋回、加速・ブレーキ・惰性を持つ。車両自動運転は目的地と道路ノードを共有し、プレイヤーが徒歩か車かを問わず起動できる。

## 都市・建物・描画

描画距離に応じて三段階の密度を維持する。近傍のCity Hub、主要店舗、公共交通、プレイヤー車両にはPBRメッシュ、ガラス、独立車輪、ライト、植栽を使う。遠景は既存のインスタンシングを維持する。全建物の完全な内部を一度に生成するのはモバイル性能を損なうため、建物種類ごとの屋内テンプレートを入口で遅延生成し、近傍で使用可能な建物に入口プロンプトを表示する。入室可能な代表カテゴリはCity Hub、カフェ、コンビニ、住宅ロビー、駅、ガレージとする。

最高品質ではPBRの金属・粗さ、近傍高解像度PCFソフト影、空・霧・雲レイヤー、強化された環境反射、近傍樹木の幹・枝・葉レイヤーを使う。中間品質は近傍影と簡略植生を保持し、最低品質でも色管理、適切な霧、基本影または接地表現を保つ。FPSはフレーム時間から実測し、draw callsは`renderer.info`から取得する。[2]

## NPCとLOCAL AI

NPCに近づいた時は文脈アクションで個別会話モーダルを開く。会話モーダルは選択中NPCの名前、人格、近況、履歴を表示し、会話ドックは街全体のログに専念する。WebLLMのモデル選択は「省メモリ」「高品質」「最高品質」を設定画面に置く。WebLLMのCache APIを利用し、ユーザーが明示して開始したモデルのみを取得する。選択済みモデルID、取得完了、永続化要求の結果を`localStorage`に保存し、Cache API内のモデルと組み合わせて次回以降の取得を避ける。[3] 上位モデルが初期化に失敗した場合は小さいモデルとテンプレート会話へ段階的にフォールバックする。

## 実装順序と制約

1. 全画面UIと詳細マップを先に導入し、既存イベントの入口を整理する。
2. HUD、モバイル、カメラを導入して、すべての機能へタッチ／キーボード双方から到達させる。
3. 車両と経路を共通化して車両自動運転と公共交通を追加する。
4. 近傍都市・屋内・品質を段階的に強化し、遠景インスタンシングを残す。
5. NPCモーダルとAIモデル選択・持続状態を追加する。

公開3Dアセットを採用する場合は、個別アセットのCC0条件を確認してから使用する。[4] この更新でもビルド不要・単一HTML・Cloudflare Pages静的配信を維持する。

## 参考資料

[1] [Roblox Creator Hub — UI and UX design](https://create.roblox.com/docs/production/game-design/ui-ux-design)

[2] [Three.js — WebGLRenderer](https://threejs.org/docs/pages/WebGLRenderer.html)

[3] [WebLLM — Basic Usage](https://webllm.mlc.ai/docs/user/basic_usage.html)

[4] [Poly Haven — Asset License](https://polyhaven.com/license)
