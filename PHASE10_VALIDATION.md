# Phase 10 — 入力復旧・ワールド参加フロー検証

## 実画面で確認した項目

ローカル環境でタイトル画面を開き、City Worldsの3種類のワールドカード、ローカルセッション人数、NPC活動量、City Score、品質ラベル、選択状態を確認した。ワールドカードからJoin Setupへ進み、描画品質、天候、時刻、交通、NPC活動の設定と「このワールドに参加」ボタンが表示されることを確認した。

タイトルおよびJoin Setupの表示中に、ゲーム中のCity Desk、HUD、Build Belt、地図、会話ドックなどの背面UIを非表示にする補正を追加した。これにより参加前の画面で操作要素が重なって見える状態を解消する。

HARBOR HEARTへの参加後に、コンパクト化した上部状況表示、右下レーダー、下部Build Belt、左下会話ドック、右側の文脈アクションを確認した。ゲームキャンバスにPointer Eventsのドラッグ入力を送信し、旧来のFree Cam専用OrbitControls依存を外した共通視点入力経路を検証対象とした。最終段階で三人称、第一人称、Free Cam、タッチ相当のPointer Eventsを含む操作を再確認する。

近接乗降ラベル、車両の状態欄、公共交通の降車案内を`E / 使う`の併記へ変更した。モバイルの常設「使う」ボタンは既存の`handlePrimaryInteract()`へ接続され、キーボードの`E`と同じ操作を実行する。

## 最終統合確認

実画面では、タイトルからHARBOR HEART、NEON AVENUE、GREEN DISTRICTを選択でき、選択後のJoin Setupで品質、天候、時刻、交通、NPC活動を設定して参加できることを確認した。HARBOR HEARTへの参加後、ゲームキャンバスをドラッグするPointer Eventsを送信し、カメラ方位が変化して近傍の車両と交差点が画面へ入ることを確認した。キャンバスは`touch-action: none`であり、中心領域のポインター対象も`game-canvas`である。

途中でPBR材質のインスタンス色設定に構文エラーが入り、モジュールが初期化されず、タイトルのボタンが反応しない状態を再現した。診断で構文エラーを特定して修正し、レンダラー、ワールド参加フロー、入力、都市生成が再起動することを確認した。材質を再調整して黒く潰れた建物を明るいファサードへ戻し、道路、屋上、窓格子、植生、車両が区別できる状態を確認した。

`validate_phase10.py`、既存の`validate_refresh.py`、JavaScript構文チェック、DOM参照、Bootstrap Icons参照、WebLLMモジュール到達性、差分形式チェックはすべて通過した。
