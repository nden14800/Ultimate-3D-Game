# Phase 3 — 全画面UI検証記録

ローカル環境でゲームを開始し、右上のポーズからCity Pause Hubを開いた。Hubはゲーム画面全体を覆う青系の全画面レイアウトで、再開、Field Pack、ワールド設定、City Atlas、乗り物、会話とAI、操作ガイド、タイトル復帰が同じ視覚言語の大きな操作カードとして表示されることを確認した。

ポーズハブから操作ガイドを開くと、旧来の小型ダイアログではなく、`INTERACTION ATLAS`という全画面ガイドが表示された。移動、交通、建築、カメラとマップ、NPCとLOCAL AI、全画面ハブの6セクションがキーボードとタッチの両方を説明し、右上の閉じる操作と下部のゲーム復帰操作を備える。

ゲーム中の常設カードは`CITY DESK`ボタンへ折り畳まれ、右側のサンドボックス操作パネルは必要な時だけ開く導線になった。WORLD READOUTは幅を拡張し、座標文字列が折り返せる設定へ変更した。

City Atlasはコンテキストの「マップ」ボタンから全画面表示できた。内部1024pxキャンバスは道路グリッド、チャンク、建物、NPC、交通、現在地を描画し、目的地の指定後は座標と5ポイントの経路を表示した。目的地設定に応じて既存の自動運転ボタンも有効になり、City Atlas内にも自動運転開始ボタンを表示することを確認した。

カメラ切替を再検証し、三人称から第一人称、Free Camへ順に切り替えられることを確認した。Free Camへの切替後は、原点ではなく、現在のプレイヤー周辺を見下ろす近距離位置から開始した。モバイル向けには使う、建築、会話、地図、視点、拡大、縮小、パック、ポーズの専用操作を追加し、キーボード入力検知時には移動用モバイルUIだけを非表示にする実装とした。

## Phase 5 検証

更新版をローカルで起動し、配達バンを選択・出現させた。車両の直上に、`E`、車両名、運転可能状態を示す近接プロンプトが表示され、コンソールの乗車ボタンから運転状態へ遷移したことを確認した。乗車後はHUDが「配達バンを運転中」と表示され、前進入力で車両位置と実測速度が更新された。シティバス、路面電車、タクシーを含む交通車両も初期シーンに生成され、公共交通用の固定循環ルートと停車タイマーを実装済みである。

## Phase 6 検証

独自生成したガラス・金属ファサード素材を、近傍の建物ストリップへ適用した。建物本体、屋上縁、屋上設備、ガラスのファサード、発光する入口を階層化し、従来の単純な立方体だけの外観を置き換えた。植生は幹と複数の葉冠を持つ立体構成に更新し、雲は品質と天候に応じて透明度を変える層として追加した。通常品質でローカル起動後の建物、屋上、植生の描画と、ブラウザコンソールに実行時エラーがないことを確認した。すべての手続き生成建物には入口座標と汎用屋内テンプレートを関連付け、近接時の`E`プロンプトから入室できる構成にした。
