# Realism and Interaction Refresh — Validation

ローカル環境でタイトル画面からプレイ画面へ遷移し、無限チャンク都市、車両、NPC、Build Belt、レーダー、拡張Conversation Dock、LOCAL AI起動導線が同時に初期化されることを確認した。City Hub用の高密度外観、衝突システム、屋内探索、入力方式切替、全画面ポーズハブ、複数NPC会話キューは、この後の操作検証対象とする。

初期プレイ画面では既存のNPC会話フォールバックが複数NPCで表示され、都市と車両の描画は継続している。構文検証、DOM参照検証、SVGスプライト参照、WebLLMモジュール取得先、差分の空白エラーは、実装直後に成功している。
ポーズボタンから、従来の小型カードではなく全画面のCity Pause Hubが表示されることを確認した。再開、Field Pack、ワールド設定、乗り物、会話とAI、操作ガイド、タイトルへ戻るの全導線が表示され、再開操作後にはプレイ画面へ戻った。既存のBuild Belt、都市、NPC、車両、会話欄も再開後に維持された。
画面操作から近傍へ車両を出現させ、乗車ボタンでクルーザーへ乗車できることを確認した。車両は独立した車輪、前後バンパー、フード、後部デッキ、ガラスキャビン、側面ガラスを持つ複合メッシュとして表示され、既存の低密度車両より外観の情報量が増えている。乗車後にUIの操作表示が「降車」へ更新されることも確認した。
車両乗車中に前進入力を与えた後、座標表示がZ=-4.0からZ=-4.8へ更新され、車両が視界の進行方向へ移動した。更新後の処理では徒歩と同じカメラ相対ベクトルを車両に使用しているため、画面上方向の前進入力が車両だけ反転する旧挙動を排除している。移動後の画面では、車両・都市・UIの描画継続を確認した。
390×844pxの実機相当キャプチャでは、Build Beltの8操作、ジョイスティック、走るボタン、追加の「使う・建築・会話」ボタン、右側の視点・車両・マップ操作、レーダーが同時に画面内へ収まった。会話欄はモバイルでは既定で折り畳まれ、下部中央の開閉ボタンのみ表示されるため、常時表示で建築帯や移動操作を覆う問題は発生していない。展開状態の高さは追加で確認する。
展開時の会話欄に基礎スタイルの`translateX(-50%)`が残り、横幅が半分に見える問題を確認して修正した。モバイル上書きで変形を無効化した後、会話ドックは左右安全領域内で全幅表示され、ログと送信欄が見切れずに表示された。追加操作ボタンは会話ドックより下のレイヤーへ配置し直し、展開中に会話内容や送信欄を覆わないことを確認した。最終構文・DOM参照・アイコン・WebLLM取得先・差分空白検証も成功している。
City Hub入口へのテレポートと入室操作を実行し、屋内専用の床・壁面へ遷移する状態を確認した。現時点でも屋内空間は生成されるが、初期カメラ位置からロビーの家具・意匠が読み取りにくいため、入口正面のレセプション、窓、植栽、照明、サインを前景へ追加して視認性を強化する。
屋内専用の近距離カメラ設定を追加し、入室時に三人称カメラが屋内壁の外側へ出る状態を解消した。再検証では、プレイヤー、レセプションカウンター、室内の青いアクセント面がカメラ内へ表示され、屋外と異なる屋内ロビー空間へ遷移することを確認した。退出時には通常のカメラ距離と向きへ戻す実装としている。
