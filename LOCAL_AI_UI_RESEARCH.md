# ブラウザ内ローカルAIとブロックサンドボックスUI — 調査記録

## ブラウザ内ローカルAI

WebLLMはWebGPUを用いて、サーバーを介さずにブラウザ内でLLM推論を行うランタイムである。Web Workerを通じて重い推論をUIスレッドから分離でき、OpenAI互換のチャット補完APIを提供する。[1]

本ゲームでは、NPC会話を**任意で起動するブラウザ内ローカルAIモード**として設計する。APIキー、バックエンド、サブスクリプションを必要としない。一方で、推論モデルは初回にブラウザへ取得され、WebGPU対応と十分な端末メモリを要する。未対応環境、取得失敗、またはモデルロード中には、会話テンプレートへ安全にフォールバックする。従って「無制限・無料」は、外部APIの呼び出し回数ではなく、ユーザー端末でのローカル実行を指す。端末性能、WebGPU、初回モデル取得は依然として制約となる。

## 独自のブロックサンドボックスUI

既存作品固有の意匠・名称・アセットを使わず、一般的なゲームUIの原則だけを採用する。常時表示するものはプレイヤーの現在の判断に直結する最小限の要素とし、詳細なワールド・建築・NPC設定はコンテキストパネルへ退避させる。これは、重要情報を優先し、状況に応じて必要なボタンのみを示すというUI・UX原則と整合する。[2]

### 設計方針

| 領域 | 独自UI方針 |
|---|---|
| クイックバー | 画面下中央に8枠の「Build Belt」を配置。素材・ツール・車両・NPC操作の切替を数字キーとクリックで行う。 |
| 建築 | 選択中の素材をゴースト表示し、左クリックで配置、右クリックで除去。既存のランダムブロック配置を素材別・向き別の操作へ拡張する。 |
| インベントリ | `Tab`で開く「Field Pack」。素材、設計図、ワールドツールをタブで整理し、画面を占有しない。 |
| HUD | 左上に位置・ワールド・時間、右上にレーダー、右側に近接NPCと会話アクション。頻度の高い操作を優先する。 |
| NPC会話 | 広い会話ドロワーを画面左下に配置し、ストリーミング回答、接続状態、ローカルモードとフォールバックを明示する。 |
| 視覚表現 | タイトル背景と同じ青空・暖色太陽・水辺・鮮やかな低ポリ建築を、プレイ可能な都市にも取り込む。画像をプレイ画面へ貼り付けない。 |

## 参照

[1] [WebLLM Documentation](https://webllm.mlc.ai/docs/)

[2] [Roblox Creator Hub — UI and UX design](https://create.roblox.com/docs/production/game-design/ui-ux-design)

## 実装APIの確認

WebLLM公式の基本使用法では、`CreateMLCEngine()`または`MLCEngine.reload()`でモデルを非同期に初期化し、初回ロードにはモデル取得のため時間がかかると明記されている。`engine.chat.completions.create()`はOpenAI形式のメッセージ配列を受け、`stream: true`を指定すれば非同期イテレータで応答を逐次描画できる。[3]

本実装では、ゲーム開始時に自動で大きなモデルを読み込まない。会話ドロワーの「LOCAL AIを開始」を明示的に操作した場合のみ、WebGPU対応を検査して動的import・モデル取得を開始する。読み込み進捗をチャット欄へ表示し、失敗時は端末側のテンプレート会話に切り替える。

[3] [WebLLM — Basic Usage](https://webllm.mlc.ai/docs/user/basic_usage.html)

## 採用モデルと実装確認

WebLLM公式のモデル設定には、`Qwen2.5-0.5B-Instruct-q4f16_1-MLC`が低リソース対応として登録され、必要VRAMは約944.62MBと記載されている。[4] 本ゲームではこの小型指示追従モデルを、ユーザーがConversation Dockの「LOCAL AIを開始」を押した場合に限り動的に読み込む。

ローカル起動時の画面には、初期状態として「ローカルAI: 未起動」と明示的な起動ボタンが表示されることを確認した。ゲーム開始やページ初期表示ではモデルの取得を行わない。

[4] [WebLLM official model configuration](https://raw.githubusercontent.com/mlc-ai/web-llm/main/src/config.ts)
