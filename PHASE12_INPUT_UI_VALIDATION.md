# Phase 12 — 入力・近接操作・UIレイアウトの検証記録

## 修正対象

車の近くで`E`を押した場合に建物へ入ってしまう問題、Shiftを離しても走り続ける問題、テキスト入力時の画面移動、チャットとインベントリの位置競合、全画面City Atlasとポーズ画面の表示範囲を修正した。

## 実装内容

近接操作には車両候補の距離とカメラ正面方向を用いた優先判定を追加した。徒歩状態で車両が至近距離、または画面正面寄りにある場合、`E`とモバイルの「使う」は建物入口・NPCより車両の乗車／運転を先に処理する。建物プロンプトも同じ判定中は表示しない。

走行状態は、毎フレームの`Shift`押下またはタッチ走るボタンのポインター保持状態から再計算するよう変更した。`keyup`、`pointerup`、`pointercancel`、`lostpointercapture`、ウィンドウのフォーカス離脱で走行状態を解除するため、キーを離した後の加速が残らない。

ゲーム外の`html`と`body`は固定表示とオーバースクロール抑止を適用し、入力欄、会話ログ、設定カード、フィールドパック、Atlasツールバーだけを局所的にスクロール可能にした。会話欄はBuild Beltの上へ配置し、全画面ポーズは`100dvh`、City Atlasは固定レイヤーかつHUDより上の`z-index: 170`とした。Atlasは説明カラムをゲーム中には表示せず、横スクロール可能なツールバー、拡大した地図領域、下部の目的地・自動運転操作に整理した。

## 実行結果

| 確認項目 | 結果 |
|---|---|
| JavaScript構文 | 合格 |
| Phase 10の視点・モバイル・Atlas検証 | 合格 |
| Phase 11のCC0モデル統合検証 | 合格 |
| スポーツカー生成後の`E`操作 | 乗車へ遷移し、入室しないことを確認 |
| 車両操作中のHUD状態 | 「降車」表示と`0 km/h`待機状態を確認 |
| City Atlasの前面レイヤー | `fixed`、`z-index:170`、HUDの`z-index:41`より前面を確認 |
| Atlasの表示範囲 | 1280×1100の全画面領域を占有することを確認 |

