# モバイルUI・タッチ入力 修正検証

## 実施した対策

ページ自体を固定ビューポートにし、`overflow: hidden`、`overscroll-behavior: none`、動的ビューポート高を使って、スマートフォンでの縦方向バウンス・横方向の画面移動を抑制した。三次元キャンバスには`touch-action: none`を指定し、タッチポインタはキャプチャして視点・建築入力として扱う。画面外や背景をスワイプした場合も、UI入力欄・スクロール可能な設定パネル・会話ログ以外ではブラウザの既定ジェスチャーをキャンセルする。

小画面ではBuild Beltを横スクロール可能な安全領域に収め、各スロットを52pxへ縮小した。会話ドックは画面下のBuild Beltより上に配置し、初期状態では折り畳む。利用者が会話ボタンを押した場合は展開状態を保持する。レーダー、Context Actions、ジョイスティック、走るボタンもsafe-areaと小画面向け寸法へ調整した。

## 確認結果

ローカル環境でタイトル画面からプレイ画面へ遷移し、既存のWorld Readout、Build Belt、会話ドック、レーダー、Context Actions、NPC会話、車両、都市描画が引き続き起動することを確認した。JavaScript構文、DOM参照、SVGアイコン参照、WebLLMモジュールURL、Git差分の空白エラーも検証済みである。

## 390px幅の視覚確認と追加調整

390×844pxのキャプチャではタイトル画面は横はみ出しなく収まった。一方、プレイ画面では折り畳み状態のConversation Dockが画面幅いっぱいのヘッダーを残すため、Build Beltおよびレーダーの上端と近接して見えた。また、52pxのBuild Beltスロットでは右側のユーティリティ操作が横スクロール領域の外に隠れる状態だった。

この結果を受け、モバイルの折り畳みConversation Dockは、ジョイスティックの右隣に置く44pxの会話起動ボタンへ縮小する。展開時だけ広い会話欄を表示する。Build Beltは8操作すべてを最低43pxのタップ領域で横一列に収め、レーダーとContext Actionsを会話ボタンから独立させる追加調整を適用する。

最終390×844pxキャプチャでは、タイトル画面およびプレイ画面の横はみ出しを確認しなかった。World Readout、上部操作、Context Actions、City Radar、ジョイスティック、走るボタン、会話起動ボタン、Build Beltが安全領域内に収まった。Build Beltは8操作すべてが同時に表示され、会話ドックは展開を要求されるまで44pxの起動ボタンとして扱われるため、建築操作とレーダーを覆わない。
実行時のタッチイベント検証では、`body`は`position: fixed`、`overflow: hidden`、`overscroll-behavior: none`となり、三次元キャンバスは`touch-action: none`であることを確認した。背景キャンバスに送ったキャンセル可能な`touchmove`は`preventDefault`され、会話ログに送った同イベントはキャンセルされなかった。したがって、背景を指でドラッグしてもページ移動を起こさず、会話ログ・設定パネル・Field Packのような明示的なスクロール領域は利用できる。
